# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# ---------------------------------------------------------------------------
# Data — source data, media, and generated artifacts are NOT tracked
# ---------------------------------------------------------------------------
# Raw downloaded datasets (too large for GitHub)
data/raw/*
!data/raw/*/.gitkeep

# Downloaded / generated images
data/media/*
!data/media/*/.gitkeep
# Scale-B source images are provenance-bound and MUST be tracked so the
# LLM-judge pipeline can rerun with the exact image bytes.
!data/media/source/
data/media/source/*
!data/media/source/mtmcs_type_b_*_main.png

# Generated family data
data/families/*
!data/families/*/.gitkeep
data/splits/*
!data/splits/.gitkeep
data/manifests/*
!data/manifests/.gitkeep

# Normalized data (may contain references to local paths)
data/normalized/*.jsonl
!data/normalized/.gitkeep

# ---------------------------------------------------------------------------
# Outputs
# ---------------------------------------------------------------------------
outputs/inference/*
!outputs/inference/.gitkeep
outputs/evaluation/*
!outputs/evaluation/.gitkeep
# Replay runs are tracked evidence; GPU-wait logs are not
outputs/replay_runs/*.log

# Keep schema reports (small, useful for reference)
# outputs/schema/ is tracked

# ---------------------------------------------------------------------------
# Misc
# ---------------------------------------------------------------------------
*.log
.DS_Store
Thumbs.db
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage

# Hugging Face cache
.cache/

# Conda
.conda/

# Local CI-reproduction venv
.ci-venv/
